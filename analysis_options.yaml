include: package:leancode_lint/analysis_options.yaml

custom_lint:
  rules:
    - use_design_system_item:
      MaatScaffold:
        - instead_of: Scaffold
          from_package: flutter

analyzer:
  plugins:
    - custom_lint
  exclude:
    - "**.g.dart"
